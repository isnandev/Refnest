{
  "name": "tauri-effect-starter",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "bun run sidecar:build && bun run --filter @starter/desktop tauri:dev",
    "build": "bun run sidecar:build && bun run --filter @starter/desktop tauri:build",
    "sidecar:build": "bun scripts/build-sidecar.ts",
    "server:dev": "bun run --filter @starter/server dev",
    "smoke": "bun scripts/smoke-sidecar.ts",
    "typecheck": "tsc -p tsconfig.json && bun run --filter '@starter/*' typecheck",
    "test": "bun run --filter '@starter/*' test",
    "check:rust": "cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml"
  },
  "dependencies": {
    "@starter/contracts": "workspace:*",
    "effect": "^3.22.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "~5.8.3"
  }
}
